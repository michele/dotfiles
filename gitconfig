[color]
    diff = auto
    status = auto
    branch = auto
    interactive = auto
[user]
    name = Michele Finotto
    email = m@finotto.org
[alias]
    st = status
    ci = commit
    co = checkout
    br = branch
    sm = submodule
    mg = merge
    sl = log --oneline --decorate -20
    sla = log --oneline --decorate --graph --all -20
    slap = log --oneline --decorate --graph --all
    slp = log --oneline --decorate
    uncommit = reset --soft HEAD^
    unstage = reset
[merge]
    summary = true
[core]
    excludesfile = /home/michele/.gitignore
[apply]
    whitespace = nowarn
[push]
    default = simple

